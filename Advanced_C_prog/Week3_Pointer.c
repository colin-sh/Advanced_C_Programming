#include <stdio.h>
/*
int main() {
	int num1, num2, *p = &num1;
	*p = 3000;
	num2 = *p;
	p = &num2;
	*p = *p - 1000;
	num1 = *p * 2;
	printf("%d %d %p", num1, num2, p);
	printf("%p %p %p", &num1, &num2, &p);
	return 0;
}
*/